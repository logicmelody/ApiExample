import {
	StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
	container: {
		flex: 1,
	},

	contentContainer: {
		flex: 1,
		padding: 20,
		justifyContent: 'center',
		alignItems: 'center',
	},

	responseText: {
		marginTop: 20,
		padding: 10,
		borderWidth: 1,
		borderColor: 'black',
	},
});

export default styles;
